#include<stdio.h>
int f(int x,int n){
int ans=1;
for(int i=0;i<n;i++)
ans*=x;
return ans;
}
int main(){
int x,n;
scanf("%d%d",&x,&n);
for(int i=1;i<=n;i++)
    printf("%d ^ %d : %d",x,i,f(x,i));
return 0;
}
